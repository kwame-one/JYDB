'use strict';

module.exports = function () {
    var data = {};

    return function (value) {
        if (!arguments.length) return data.value;

        data.value = value;

        return value;
    };
};